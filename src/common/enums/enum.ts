export enum RoleEnum {
  SUPERADMIN = 'superAdmin',
  ADMIN = 'admin',
  MANAGER = 'manager',
  WORKER = 'worker'
}

export enum StatusEnum {
  PROCESS = 'process',
  DONE = 'done',
  TOOK = 'took'
}
